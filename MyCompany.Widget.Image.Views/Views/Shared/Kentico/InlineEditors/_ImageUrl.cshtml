@using Kentico.PageBuilder.Web.Mvc

@model MyCompany.Widget.Image.Models.ImageWidgetViewModel

@using (Html.Kentico().BeginInlineEditor("Company.InlineEditor.ImageUrl", nameof(Model.ImageUrl)))
{
    <div class="cmp-image-url">
        <form>
            <input type="search" name="imageUrl" value="@Model.ImageUrl" placeholder="Insert image URL" />
            <button class="cmp-image-url-apply">🗸</button>
        </form>
    </div>
}
